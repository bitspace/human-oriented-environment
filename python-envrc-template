# Template .envrc file for Python projects
# Copy this to your Python project directory as .envrc
# Then run: direnv allow

# Use Python with automatic virtual environment management
layout python_auto

# Export any project-specific environment variables
# export PYTHONPATH="${PWD}/src:${PYTHONPATH}"
# export JUPYTER_CONFIG_DIR="${PWD}/.jupyter"

# Add project bin to PATH if it exists
if [ -d "${PWD}/bin" ]; then
    PATH_add bin
fi

# Add .venv/bin to PATH (redundant but ensures it's first)
PATH_add .venv/bin